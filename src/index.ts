import { import as _import, export as _export, throw as _throw } from './mod';
import config from './config';

const mod = {
    import: _import,
    export: _export,
    throw: _throw,
    config
};

export default mod;
